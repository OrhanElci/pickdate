function selectActivity(activity) {
  const result = document.getElementById("result");
  result.textContent = `Yay! Can't wait for our ${activity}! ❤️`;
}
function selectActivity(activity) {
  const result = document.getElementById("result");
  result.textContent = `Yaşasın! Bugün ${activity} yapıyoruz! 💞`;
}
